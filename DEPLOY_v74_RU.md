# Kitobkhona v74 — ислоҳи сарлавҳа + навсозии надписҳои splash

В архиве ТОЛЬКО изменённые файлы (замена поверх любой версии v46–v73):

```
kitobkhona/index.html   — сарлавҳа: надписи КИТОБХОНА дигар не перекрывается;
                           splash: КИТОБХОНА (тиллоӣ, градиент) + МАНБАИ ДОНИШ
kitobkhona/sw.js        — CACHE_NAME → 'kitobkhona-v74-brand-header-fix'
```

`splash_logo.png` в этой версии НЕ менялся — картинка остаётся от v73.

## 1. Сарлавҳа (верх слева) — надпись больше не срывается

Причина: логотип мог сжиматься (`min-width:0`), а виджет обу ҳаво его перекрывал —
надпись КИТОБХОНА уезжала под виджет на 33 px (последние буквы срезались).

Исправлено:

- логотип закреплён (`flex-shrink:0`) — заголовок всегда виден целиком;
- теперь сжимается правая часть, а не логотип;
- виджет даты/погоды стал компактнее, на узких экранах имя города скрывается
  (остаётся «☀️ +24°»; полный прогноз — по нажатию, как раньше);
- подзаголовок в шапке: «Китоб манбаи дониш» → «Манбаи дониш».

Замер перекрытия: было +33 px (перекрыто) → стало −46 px (зазор) на 412 px;
на 360 px и 320 px — ровно 0. Проверено на 4 ширинах экрана.

## 2. Надписи на splash — красивый шрифт

- **КИТОБХОНА** — золотой градиент по буквам (светлое золото → насыщенное),
  крупно (27 px), с широкой разрядкой букв и мягким свечением;
- **МАНБАИ ДОНИШ** — вместо «Китоб манбаи дониш»; заглавными буквами, с разрядкой,
  по бокам тонкие золотые линии-орнамент;
- логотип и время показа (5 секунд) не менялись.

## Service Worker

`CACHE_NAME` → `kitobkhona-v74-brand-header-fix` — после публикации клиенты получат
обновления сразу.

## Проверено

- шапка: перекрытие устранено на 412/390/360/320 px, полоса не вылезает;
- splash: заголовок с градиентом, подзаголовок «Манбаи дониш» с орнаментом,
  всё помещается по ширине, показ 5 секунд;
- приложение работает (раздел Пешвои миллат — 12 карточек), JS-ошибок нет.
