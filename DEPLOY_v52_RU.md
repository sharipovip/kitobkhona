# Kitobkhona v52 — splash: тоза (бе ҳалқа), логотипи нави векторӣ, 5+ сония

В архиве ТОЛЬКО изменённые файлы (замена поверх v46–v51):

```
kitobkhona/index.html        — splash: ҳалқа хориҷ, ҳадди ақал 5 сония намоиш
kitobkhona/splash_logo.png   — логотипи НАВ: тоҷи тиллоӣ, векторӣ, 1024×1024
kitobkhona/sw.js             — CACHE_NAME → 'kitobkhona-v72-splash-vector'
```

## Что сделано

### 1. Кольцо вокруг фото убрано
Вращающееся кольцо флага удалено полностью — чистый круглый логотип на тёмном
фоне, только мягкая тень для глубины. Замер: за пределами круга 0.00% цветного.

### 2. Логотип нарисован заново — векторное качество
Старый `splash_logo.jpg` (250×250) был снят с маленькой картинки — качество
терялось при увеличении. Новая эмблема — **та же золотая корона с красным, белым
и зелёным камнями** (фирменный знак из шапки приложения), отрисованная напрямую
из векторного SVG в разрешении **1024×1024**:

- резкость краёв 83 против 30–60 у старого JPG;
- при увеличении до 80% экрана остаётся идеально чёткой;
- размер файла всего 125 КБ.

### 3. Логотип большой — 80% ширины экрана
330 px на экране (было 300 с кольцом, изначально 170).

### 4. Splash стоит минимум 5 секунд
Раньше скрывался через ~1.7 с после загрузки. Теперь:

- минимум **5 секунд** — даже если программа загрузилась мгновенно;
- если загрузка дольше — splash остаётся до её окончания;
- максимум 9 секунд (страховка, чтобы никогда не завис).

## Service Worker

`CACHE_NAME` → `kitobkhona-v72-splash-vector` — клиенты получат новый логотип
сразу после публикации.

## Проверено

- логотип 330 px (80% экрана), натуральное разрешение 1024;
- кольца нет (0.00% цветного за кругом);
- splash виден на 3-й и 4.8-й секунде, исчезает после 5-й;
- приложение после splash работает: раздел Пешвои миллат — 12 карточек;
- JS-ошибок нет.
